name: CI/CD Pipeline

on: 
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build-and-test:
        runs-on: ubuntu-latest
        steps:
            - name: Check out code
              uses: actions/checkout@v2

            - name: Set up Docker Build
              run: docker build -t ping-monitor .
            
            - name: Run Test in Docker
              if: success()
              run: docker run ping-monitor pytest test_ping_service.py
